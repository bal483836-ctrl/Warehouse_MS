# 仓库管理系统 —— 功能扩展方案

> 配套脚本：`ahut_base_extend.sql`（在 `ahut_base_clean.sql` 基础上执行）
> 数据库表：由 **10 张** 扩展到 **18 张**（新增 8 张）

---

## 一、扩展总览

结合本系统"食品 / 冷冻 / 果蔬"等生鲜仓库的特点，补齐了一个真实 WMS 常见的缺口。
新增 7 个功能模块、8 张表，并已挂载到 `sys_menu` 菜单树、授权给 `admin` 角色。

| # | 功能模块 | 新增表 | 解决的现有短板 | 前端组件（建议） |
| --- | --- | --- | --- | --- |
| 1 | 供应商管理 | `supplier` | 入库不知货物来源 | `SupplierManage.vue` |
| 2 | 客户管理 | `customer` | 出库不知货物去向 | `CustomerManage.vue` |
| 3 | 批次 / 保质期 | `goods_batch` | 生鲜无生产日期、保质期追踪 | `BatchManage.vue` |
| 4 | 库存预警 | `stock_alert` | 无安全库存阈值，缺货/积压无提醒 | `StockAlert.vue` |
| 5 | 库存盘点 | `stock_check`、`stock_check_item` | 无法核对系统库存 vs 实盘 | `StockCheck.vue` |
| 6 | 货位管理 | `location` | 仓库内无货架/库位定位 | `LocationManage.vue` |
| 7 | 系统公告 | `sys_notice` | 无内部通知/公告 | `NoticeManage.vue` |

---

## 二、各模块说明与关键字段

### 1. 供应商管理 `supplier`
维护货物来源单位：名称、联系人、电话、邮箱、地址。含 `del_flag` 逻辑删除与审计字段。
> 后续可在 `goods` 或 `record` 增加 `supplier_id` 建立"货物 ← 供应商"关联。

### 2. 客户管理 `customer`
维护出库去向单位/个人，字段与供应商对称。
> 后续可在出库 `record` 增加 `customer_id`。

### 3. 批次 / 保质期 `goods_batch`
按批次追踪货物：`batch_no` 批次号、`production_date` 生产日期、`expiry_date` 到期日、`supplier_id` 来源、`count` 批次数量。
> **业务价值**：果蔬/冷冻/食品仓库可做"临期预警""先进先出（FIFO）"。

### 4. 库存预警 `stock_alert`
为每种货物设置 `min_count`（缺货下限）/ `max_count`（积压上限），`enabled` 控制开关。
> 判定逻辑：`goods.count < min_count` → 缺货预警；`> max_count` → 积压预警。可配合 `图表统计` 页红色高亮。

### 5. 库存盘点 `stock_check` + `stock_check_item`
- 主表 `stock_check`：盘点单号、仓库、盘点人、状态（0进行中/1已完成）。
- 明细 `stock_check_item`：`system_count` 系统库存、`actual_count` 实盘、`diff_count` 差异。
> 差异结果可反向修正 `goods.count` 并写入 `record` 流水。

### 6. 货位管理 `location`
仓库内货架/库位细分：`code` 库位编码（如 `A-01-01`）、所属 `storage_id`。
> 后续可在 `goods` 增加 `location_id` 实现精确定位。

### 7. 系统公告 `sys_notice`
内部通知/公告：标题、内容、类型（0通知/1公告）、状态（0草稿/1已发布）。

---

## 三、表关系（扩展后 E-R 概要）

```
供应商 supplier ──1:N──> goods_batch <──N:1── goods
客户   customer ──(逻辑关联)──> record（出库去向）

goods ──1:1──> stock_alert（安全库存阈值）
goods ──1:N──> goods_batch（批次）

storage ──1:N──> location（货位）
storage ──1:N──> stock_check ──1:N──> stock_check_item ──N:1──> goods

sys_notice（独立公告表）
新增 14 条 sys_menu + 14 条 sys_role_res（授权 admin）
```

---

## 四、数据库表数量对照

| 阶段 | 表数量 | 明细 |
| --- | --- | --- |
| 原始 | 10 | 4 业务 + 6 权限/系统 |
| **扩展后** | **18** | 4 原业务 + **8 新业务** + 6 权限/系统 |

新增 8 张：`supplier`、`customer`、`goods_batch`、`stock_alert`、`stock_check`、`stock_check_item`、`location`、`sys_notice`。

---

## 五、使用方式

```sql
-- 1. 先导入纯净基础库
SOURCE ahut_base_clean.sql;
-- 2. 再执行扩展脚本
SOURCE ahut_base_extend.sql;
```

脚本已通过列数/值数一致性校验（0 处不匹配），可直接导入 MySQL 8.0+。

> 说明：本脚本只完成**数据库层**扩展。若要在界面上使用，还需按 `component` 列
> 补充对应的 Vue 页面与后端 Controller/Service/Mapper（当前仓库未包含前后端源码）。

---

## 六、还可继续扩展的方向（备选）

| 方向 | 表设计思路 |
| --- | --- |
| 采购/销售订单 | `purchase_order` + `_item`、`sales_order` + `_item`（把简单 `record` 升级为单据） |
| 单位管理 | `unit`（个/箱/kg），`goods` 增加 `unit_id` |
| 调拨管理 | `transfer`（仓库间调拨单），支持库存在仓库间流转 |
| 数据字典 | `sys_dict` + `sys_dict_item`（统一维护下拉枚举） |
| 部门管理 | `sys_dept`，`sys_user` 增加 `dept_id` 做数据权限 |
