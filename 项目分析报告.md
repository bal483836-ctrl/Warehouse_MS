# 仓库管理系统 —— 项目分析报告

> 生成日期：2026-07-16　|　数据库源：`ahut_base`（MySQL 8.0.33）

---

## 一、项目整体理解

这是一个基于 **RBAC 权限模型** 的 **B/S 架构仓库管理系统（Warehouse Management System）**，
项目工程名为 `qitech-ahut-base`。

### 技术栈（从工程配置与菜单组件路径推断）

| 层次 | 技术 | 依据 |
| --- | --- | --- |
| 后端 | Java 17 + Maven + Spring Boot | `.idea/misc.xml`（`JDK_17`、`MavenProjectsManager` 指向 `pom.xml`） |
| 数据库 | MySQL 8.0.33 | `.idea/dataSources.xml`（`jdbc:mysql://localhost:3306/ahut_base`） |
| 前端 | Vue + Element UI | `sys_menu.component` 全为 `*.vue`、`Layout`；`icon` 为 `el-icon-*` |
| 密码安全 | 盐值加密（salt + hash） | `sys_user` 表含 `password` + `salt` 字段 |

### ⚠️ 重要发现：仓库中没有可运行的源代码

本仓库里 **真正的 Java / Vue 源代码目录是空的**：

- `qitech-ahut-base/qitech-ahut-base/` —— 空目录（`pom.xml` 被 IDE 配置引用，但磁盘上不存在）
- `optics-management/optics-management/` —— 空目录

仓库里 **实际存在的有效内容** 只有：

1. **`ahut_base.sql`** —— 完整数据库结构 + 数据（本项目唯一可执行的“纯净代码”）
2. `.idea/*` —— IntelliJ 工程配置
3. `仓库管理系统.docx` / `ppt.pptx` —— 文档与答辩 PPT
4. `截图/`、`画图/` —— 界面截图与 E-R 图 / 流程图（源码仅以**图片**形式存在）

> 因此本次“提取纯净正确的代码”，产出物为 **`ahut_base_clean.sql`**：
> 保留全部 10 张表结构 + 全部业务/配置数据，仅剔除 `sys_log` 表中 2629 行日志噪声，
> 文件从 **520 KB 精简到 24 KB**，可直接导入运行。

---

## 二、系统功能（依据 `sys_menu` 菜单表还原）

系统采用「根菜单 → 一级菜单 → 页面/资源」的树形菜单结构，实际功能模块如下：

### 1. 业务功能（仓库核心）

| 模块 | 路由 | 前端组件 | 说明 |
| --- | --- | --- | --- |
| **出入库管理** | `/goods` | `GoodsManage.vue` | 货物的入库（0）/ 出库（1）操作 |
| **库存记录** | `/records` | `RecordManage.vue` | 查看每一笔出入库流水记录 |
| **仓库管理** | `/storage` | `StorageManage.vue` | 维护各个仓库（数码/食品/冷冻/果蔬…） |
| **商品分类管理** | `/goodsType` | `GoodstypeManage.vue` | 维护商品类别（日用品/数码/食品…） |
| **图表统计** | `/chart` | `StatisticsChart.vue` | 库存 / 出入库数据可视化图表 |
| **个人中心** | `/userInformation` | `UserInformation.vue` | 修改个人信息、密码 |

### 2. 系统管理功能（权限后台）

| 模块 | 路由 | 说明 |
| --- | --- | --- |
| **基本信息管理** | `/setting` | 系统设置父菜单 |
| └ 菜单管理 | `/setting/menu` | 维护系统菜单树 |
| └ 角色管理 | `/setting/role` | 维护角色及其资源权限 |
| └ 用户管理 | `/setting/userManage` | 维护系统用户、分配角色 |
| **操作日志** | `/logs` | 查看所有接口调用日志（内容/IP/耗时/结果） |

### 3. 登录认证

- 账号密码登录，密码采用 **salt 盐值 + hash** 存储（非明文）
- 内置账号：`admin`（超级管理员）、`test`（测试用户）

---

## 三、数据库表：共 **10 张表**

数据库 `ahut_base` 共有 **10 张表**，可分为「业务表（4）」与「权限/系统表（6）」两类。

### A. 业务表（4 张）

| # | 表名 | 中文含义 | 关键字段 |
| --- | --- | --- | --- |
| 1 | `goods` | 货物表 | name(货名)、storage(所属仓库)、goodsType(分类)、count(数量)、remark |
| 2 | `goodstype` | 商品分类表 | name(分类名)、remark |
| 3 | `storage` | 仓库表 | name(仓库名)、remark |
| 4 | `record` | 出入库记录表 | goods(货品id)、user_id(操作人)、count(数量)、createtime、type(0入库/1出库) |

### B. 权限 / 系统表（6 张，标准 RBAC 模型）

| # | 表名 | 中文含义 | 说明 |
| --- | --- | --- | --- |
| 5 | `sys_user` | 用户表 | 账号、密码、salt、锁定/删除标识、创建/更新审计字段 |
| 6 | `sys_role` | 角色表 | 角色名、描述 |
| 7 | `sys_menu` | 菜单表 | 菜单树（type: 0菜单/1页面/2资源）、组件路径、图标、排序 |
| 8 | `sys_user_role` | 用户-角色关联表 | user_id ↔ role_id |
| 9 | `sys_role_res` | 角色-资源关联表 | role_id ↔ res_id（res_type: 0菜单/1页面/2资源） |
| 10 | `sys_log` | 操作日志表 | 操作内容、IP、参数、方法路径、结果、耗时 |

### 表关系（E-R 概要）

```
业务侧：
  storage(仓库) ──1:N──> goods(货物) <──N:1── goodstype(分类)
  goods(货物)   ──1:N──> record(出入库记录) <──N:1── sys_user(操作人)

权限侧（RBAC）：
  sys_user ──< sys_user_role >── sys_role ──< sys_role_res >── sys_menu
  sys_user ──1:N──> sys_log（谁操作了什么）
```

---

## 四、数据现状（去噪前）

| 表 | 数据行数 |
| --- | --- |
| goods | 9 |
| goodstype | 7 |
| storage | 8 |
| record | 22 |
| sys_menu | 21 |
| sys_role | 3 |
| sys_role_res | 43 |
| sys_user | 2 |
| sys_user_role | 3 |
| **sys_log** | **2629（已在 clean 版中清空）** |

---

## 五、结论

- **功能**：登录认证 + 6 个业务模块（出入库、库存记录、仓库、分类、图表统计、个人中心）+ 4 个系统管理模块（菜单、角色、用户、日志）。
- **数据库表数量**：**10 张**（4 张业务表 + 6 张权限/系统表）。
- **纯净代码产出**：`ahut_base_clean.sql`（24 KB，10 张表结构 + 全部业务/配置数据，已剔除日志噪声，可直接导入）。
- **注意**：仓库内 **未包含后端 Java 与前端 Vue 源码**（仅有截图），若需完整运行系统，需要补充这两部分源代码工程。
