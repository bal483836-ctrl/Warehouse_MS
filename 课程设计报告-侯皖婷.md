# 仓库管理系统 课程设计报告

**姓名：** 侯皖婷　**班级：** 计231　**承担模块：** 前端样式设计；后端登录认证、用户管理、角色管理、菜单管理

**项目名称：** 仓库管理系统（库智 WMS）　**技术栈：** Java 17 · Spring Boot 3.3 · Spring Data JPA · MySQL 8 · 原生 SPA 前端

---

## 一、课程设计任务及需求分析

### 1.1 课程设计任务

本课程设计以团队协作方式完成一套面向超市 / 日用仓库的**进销存仓库管理系统**，实现从商品建档、出入库作业、库位存储到数据统计预警的全流程管理，并配套一套基于 RBAC（Role-Based Access Control，基于角色的访问控制）模型的权限后台。

在团队分工中，本人（侯皖婷）承担以下两大板块：

1. **前端样式设计**——搭建全站统一的视觉设计规范（配色、排版、组件样式、响应式布局），保证登录页与主控台风格一致、专业美观；
2. **后端系统管理与安全模块**——包括**登录认证**、**用户管理**、**角色管理**、**菜单管理**四个功能，构成整个系统的"入口"与"权限地基"。

其余业务模块（出入库、订单、货位、统计等）由团队其他成员完成，本报告只对本人负责的部分展开详细设计。

### 1.2 需求分析

#### 1.2.1 功能性需求

| 编号 | 模块 | 需求描述 |
| --- | --- | --- |
| F1 | 登录认证 | 用户凭"账号 + 密码"登录；密码不得明文存储与传输校验；登录成功后签发令牌（Token），后续请求凭令牌访问；支持退出登录、查询当前登录人、修改密码 |
| F2 | 访问控制 | 除登录接口外，所有接口必须携带有效 Token 才能访问；令牌失效或未登录时统一返回未授权（401），前端自动跳回登录页 |
| F3 | 用户管理 | 对系统用户进行新增、查询、修改、删除（增删改查，CRUD）；可维护姓名、账号、年龄、性别、电话，可锁定 / 逻辑删除账号 |
| F4 | 角色管理 | 维护系统角色（如超级管理员、仓管员、测试角色）；为用户分配角色；为角色分配可访问的资源 |
| F5 | 菜单管理 | 维护系统菜单树（菜单 / 页面 / 资源三级），支持图标、排序、组件路径、父子层级，驱动前端导航与授权 |
| F6 | 前端样式 | 提供统一的深色侧边栏 + 浅色内容区的后台管理风格；登录页为左右分栏式；具备一致的按钮、输入框、卡片、状态色与响应式适配 |

#### 1.2.2 非功能性需求

- **安全性**：口令采用"加盐散列"存储，杜绝明文；接口统一鉴权；返回给前端的用户对象剔除密码与盐值。
- **一致性**：前端所有页面复用同一套设计变量（Design Token），风格统一、易维护。
- **易用性**：登录有加载态与错误提示，操作有轻提示（Toast），关键操作有确认。
- **可维护性**：后端采用分层结构，系统管理类接口复用同一套通用 CRUD 逻辑，减少重复代码。
- **兼容性**：前端为原生实现、无需构建工具，PC 与移动端自适应。

---

## 二、总体设计方案

### 2.1 系统总体架构

系统采用**经典三层 + 前后端一体**的架构，自上而下分为表现层、应用层、数据层：

```
┌───────────────────────────────────────────────┐
│  表现层  浏览器 · 原生 SPA                        │
│  login.html / index.html / app.js / app.css     │
└───────────────┬───────────────────────────────┘
                │ HTTP / JSON（请求头携带 token）
┌───────────────▼───────────────────────────────┐
│  应用层  Spring Boot 3.3                         │
│  ┌─────────────────────────────────────────┐   │
│  │ 拦截器：AuthInterceptor（鉴权）          │   │
│  │         AccessLogInterceptor（操作日志） │   │
│  ├─────────────────────────────────────────┤   │
│  │ Controller：Auth / SysUser / SysRole /   │   │
│  │             SysMenu / SysRoleRes …       │   │
│  ├─────────────────────────────────────────┤   │
│  │ common：Result 统一返回 · Md5Util 散列   │   │
│  │ security：TokenStore 令牌存储            │   │
│  ├─────────────────────────────────────────┤   │
│  │ Repository（Spring Data JPA）            │   │
│  └─────────────────────────────────────────┘   │
└───────────────┬───────────────────────────────┘
                │ JDBC · Hibernate ORM
┌───────────────▼───────────────────────────────┐
│  数据层  MySQL 8（库：ahut_base）               │
│  权限核心表：sys_user / sys_role / sys_menu /   │
│              sys_user_role / sys_role_res       │
└───────────────────────────────────────────────┘
```

**技术选型说明：**

| 层次 | 技术 | 选型理由 |
| --- | --- | --- |
| 前端 | 原生 HTML/CSS/JS（SPA） | 无需 Node 构建、体积小、由 Spring Boot 直接托管，便于教学演示 |
| 后端 | Spring Boot 3.3 + Spring Data JPA | 约定优于配置、开发效率高，JPA 免写样板 SQL |
| 数据库 | MySQL 8 | 成熟稳定、支持中文与事务，符合课程要求 |
| 鉴权 | 加盐散列 + 内存 Token | 实现简单、够用，直观展示认证与授权原理 |

### 2.2 本人负责模块在总体中的定位

登录认证与用户 / 角色 / 菜单管理共同构成系统的 **RBAC 权限骨架**，是所有业务模块的"守门人"：任何业务接口都必须先通过登录认证，再由角色决定能访问哪些菜单和资源。前端样式则是所有页面共享的"皮肤"，决定了系统整体的观感与交互一致性。

### 2.3 数据库设计（核心表）

数据库名为 `ahut_base`，整体包含业务表（商品、仓库、货位、出入库记录、订单等）与权限系统表两大类。**本人负责模块直接对应的 5 张权限核心表**及其关系如下，是本设计的核心数据模型。

#### 2.3.1 核心表结构

**① `sys_user` 用户表**——存储登录账号与个人信息，密码以"散列 + 盐"保存。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int PK | 主键，自增 |
| name | varchar(32) | 姓名 |
| number | varchar(32) | **登录账号**（唯一登录凭据） |
| password | varchar(100) | **口令散列值**（非明文）= md5(md5(明文)+salt) |
| salt | varchar(32) | **密码盐**，每用户独立 |
| age / sex / phone | — | 年龄 / 性别(1男0女) / 电话 |
| locked | tinyint | 是否锁定（1 锁定，0 正常） |
| loginsign | tinyint | 是否初次登录 |
| del_flag | tinyint | 逻辑删除标识（1 删除，0 正常） |
| create_time / create_by / update_time / update_by | — | 审计字段 |

**② `sys_role` 角色表**——定义系统角色。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int PK | 主键 |
| name | varchar(50) | 角色名称（如超级管理员、仓管员） |
| description | varchar(100) | 角色描述 |
| del_flag / create_time / … | — | 逻辑删除与审计字段 |

**③ `sys_menu` 菜单表**——树形菜单，驱动前端导航与授权粒度。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int PK | 主键 |
| name | varchar(50) | 菜单名称 |
| parent_id | int | 上级菜单 id（构成树形层级） |
| type | int | **菜单类型：0 菜单 / 1 页面 / 2 资源** |
| icon / url / component | — | 图标 / 目标地址 / 前端组件路径 |
| sort | int | 排序序号 |
| is_hidden_menu / is_option_menu | tinyint | 是否隐藏 / 是否下拉子菜单 |
| del_flag / 审计字段 | — | — |

**④ `sys_user_role` 用户-角色关联表**——多对多中间表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int PK | 主键 |
| user_id | int | 用户 id（→ sys_user.id） |
| role_id | int | 角色 id（→ sys_role.id） |

**⑤ `sys_role_res` 角色-资源关联表**——角色可访问哪些菜单 / 页面 / 资源。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int PK | 主键 |
| role_id | int | 角色 id（→ sys_role.id） |
| res_id | int | 资源 id（→ sys_menu.id） |
| res_type | int | 资源类型：0 菜单 / 1 页面 / 2 资源 |

#### 2.3.2 表关系（E-R 概要）

```
sys_user ──< sys_user_role >── sys_role ──< sys_role_res >── sys_menu
  （一个用户可有多个角色，一个角色可绑定多个用户；
    一个角色可授权多个菜单资源，一个菜单可授予多个角色）
```

这是一个标准的 **RBAC「用户—角色—资源」三层授权模型**：用户不直接绑定权限，而是通过"角色"这个中间层间接获得对菜单资源的访问权，从而实现权限的批量分配与灵活调整。

### 2.4 认证与授权总体流程

```
登录：账号+密码 → 查用户 → 校验锁定/删除 → 加盐散列比对 → 签发 Token → 前端保存
访问：请求头带 Token → 鉴权拦截器校验 → 通过则放行(注入登录人) / 失败返回 401
授权：登录人 → 角色(sys_user_role) → 可访问资源(sys_role_res→sys_menu) → 决定可见菜单
```

---

## 三、详细设计 / 具体实现

### 3.1 功能一：前端样式设计

#### 3.1.1 设计目标

打造一套"深色侧边导航 + 浅色内容工作区"的专业级后台管理视觉体系，登录页则采用左右分栏的沉浸式设计，做到**一套变量、全站统一**。

#### 3.1.2 设计规范（Design Token）

全站样式基于 CSS 自定义变量（`:root`）集中管理，改一处即可全站生效，核心变量如下：

```css
:root{
  --brand-1:#0a0e17; --brand-2:#0e1523;      /* 侧边栏深色 */
  --accent:#2a78d6; --accent-deep:#184f95;   /* 品牌主色（蓝） */
  --plane:#eef2f9; --surface:#fff;           /* 页面底色 / 卡片面 */
  --ink:#0f1b33; --muted:#8a99b5;            /* 主文字 / 次文字 */
  --good:#0ca30c; --warn:#e0920a; --crit:#d03b3b;   /* 语义状态色 */
  --b100…--b700;                              /* 蓝色阶，用于图表 */
  --shadow / --shadow-lg;                     /* 阴影层级 */
  --r-card:16px; --r-ctl:10px;                /* 圆角规范 */
}
```

#### 3.1.3 布局与组件实现要点

- **整体布局**：`.app` 使用 CSS Grid `grid-template-columns:248px 1fr`，左固定侧边栏、右自适应主区；侧边栏 `position:sticky` 固定，内容区可独立滚动。
- **侧边导航**：深色渐变背景，导航项含图标 + 文字；`.active` 态用左侧 3px 高亮条 + 渐变底色标识当前页，交互反馈清晰。
- **顶栏 Topbar**：`position:sticky` 吸顶 + `backdrop-filter:blur(10px)` 毛玻璃效果，滚动时保持标题可见。
- **通用组件**：统一封装 `.btn` 按钮、`.chip` 标签、`.card` 卡片、`.field` 表单项、`.spin` 加载动画等，各页面直接复用。
- **登录页样式**（login.html 内联）：左右分栏（`.left` 表单 46% + `.right` 配图），输入框 `:focus-within` 聚焦高亮，登录按钮为渐变色 + 悬浮上移动效；`@media (max-width:820px)` 下隐藏右侧配图、表单占满，实现移动端适配。
- **响应式**：主控台在窄屏下侧边栏转为可收起的抽屉（配合 `.menu-btn` 与 `.nav-backdrop`），保证小屏可用。

#### 3.1.4 前端与鉴权的衔接

登录成功后，令牌与用户信息存入浏览器本地存储：

```js
localStorage.setItem('wms_token', j.data.token);
localStorage.setItem('wms_user', JSON.stringify(j.data.user));
```

主控台 `app.js` 在每次请求时自动携带令牌，并对未授权响应做统一跳转：

```js
const r = await fetch(path, { headers:{ 'Content-Type':'application/json', token: TOKEN } });
if (r.status === 401) { localStorage.clear(); location.href = 'login.html'; }
```

### 3.2 功能二：登录认证

#### 3.2.1 口令加盐散列（Md5Util）

系统不存储明文密码，散列方案为 **`md5( md5(明文密码) + 盐 )`**：

```java
public static String hashPassword(String rawPassword, String salt) {
    return md5(md5(rawPassword) + salt);
}
```

每个用户拥有独立 `salt`，即便两人密码相同，入库散列值也不同，可有效抵御彩虹表攻击。

#### 3.2.2 登录接口（AuthController#login）

登录流程：**校验非空 → 按账号查用户 → 判断是否删除 / 锁定 → 加盐散列比对 → 签发令牌**：

```java
Optional<SysUser> opt = userRepo.findByNumber(req.number().trim());
if (opt.isEmpty()) return Result.fail("账号不存在");
SysUser u = opt.get();
if (u.getDelFlag()==1) return Result.fail("账号已删除");
if (u.getLocked()==1)  return Result.fail("账号已被锁定");
String hashed = Md5Util.hashPassword(req.password(), u.getSalt());
if (!hashed.equalsIgnoreCase(u.getPassword())) return Result.fail("密码错误");
String token = tokenStore.issue(new Principal(u.getId(), u.getName(), u.getNumber()));
```

返回给前端的用户对象经 `safeUser()` 处理，**剔除了 password、salt 等敏感字段**，只保留 id、姓名、账号、年龄、性别、电话。

#### 3.2.3 令牌存储（TokenStore）

采用轻量的**内存令牌**方案：登录时生成不带连字符的 UUID 作为 Token，以 `ConcurrentHashMap` 维护"Token → 登录人"的映射，支持并发访问：

```java
public String issue(Principal p){ String t=UUID.randomUUID().toString().replace("-",""); tokens.put(t,p); return t; }
public Principal resolve(String t){ return t==null?null:tokens.get(t); }
public void revoke(String t){ if(t!=null) tokens.remove(t); }   // 退出登录
```

#### 3.2.4 统一鉴权拦截（AuthInterceptor + WebConfig）

`AuthInterceptor` 在请求进入控制器前统一校验令牌，通过则把登录人注入请求上下文，失败则返回 401：

```java
String token = req.getHeader("token");
Principal p = tokenStore.resolve(token);
if (p == null) { resp.setStatus(401); /* 返回“未登录或登录已过期” */ return false; }
req.setAttribute("principal", p);
```

在 `WebConfig` 中，将拦截器注册到 `/api/**`，并**放行登录接口**，实现"除登录外全站鉴权"：

```java
registry.addInterceptor(authInterceptor)
        .addPathPatterns("/api/**")
        .excludePathPatterns("/api/auth/login", "/api/auth/captcha");
```

#### 3.2.5 修改密码（AuthController#changePassword）

校验原密码正确、新密码不少于 6 位后，用原盐重新散列保存，保证改密同样不落明文。

### 3.3 功能三：用户管理

#### 3.3.1 实体映射（SysUser）

采用 JPA 注解将实体映射到 `sys_user` 表，配合 Lombok `@Data` 自动生成 getter/setter：

```java
@Data @Entity @Table(name = "sys_user")
public class SysUser {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Integer id;
    private String name; private String number;
    private String password; private String salt;
    private Integer age; private String sex; private String phone;
    private Integer locked; private Integer delFlag;
    // …审计字段
}
```

#### 3.3.2 通用 CRUD 基类（BaseController）

用户、角色、菜单等系统管理表的操作高度相似，因此抽象出泛型基类 `BaseController<T>`，统一提供"增删改查"五个接口，各控制器只需继承并注入自己的 Repository，**极大减少重复代码**：

```java
public abstract class BaseController<T> {
    protected abstract JpaRepository<T,Integer> repo();
    @GetMapping                 public Result<List<T>> list()               { return Result.ok(repo().findAll()); }
    @GetMapping("/{id}")        public Result<T> get(@PathVariable Integer id){ … }
    @PostMapping                public Result<T> create(@RequestBody T e)    { return Result.ok(repo().save(e)); }
    @PutMapping("/{id}")        public Result<T> update(@PathVariable Integer id,@RequestBody T e){ return Result.ok(repo().save(e)); }
    @DeleteMapping("/{id}")     public Result<Void> delete(@PathVariable Integer id){ repo().deleteById(id); return Result.ok(); }
}
```

用户管理控制器仅 3 行即完成全部 CRUD：

```java
@RestController @RequestMapping("/api/sys_user")
public class SysUserController extends BaseController<SysUser> {
    private final SysUserRepo repo;
    public SysUserController(SysUserRepo repo){ this.repo = repo; }
    @Override protected JpaRepository<SysUser,Integer> repo(){ return repo; }
}
```

#### 3.3.3 接口一览

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/sys_user` | 用户列表 |
| GET | `/api/sys_user/{id}` | 用户详情 |
| POST | `/api/sys_user` | 新增用户 |
| PUT | `/api/sys_user/{id}` | 修改用户（姓名 / 电话 / 锁定等） |
| DELETE | `/api/sys_user/{id}` | 删除用户 |

### 3.4 功能四：角色管理

角色管理同样复用 `BaseController`，控制器映射到 `/api/sys_role`，实体 `SysRole` 映射 `sys_role` 表，并在实体中用 `@PrePersist` 在落库前自动补齐创建时间与删除标识：

```java
@PrePersist void onCreate(){
    if (createTime == null) createTime = LocalDateTime.now();
    if (delFlag == null)   delFlag = 0;
}
```

**用户 ↔ 角色**的分配通过 `sys_user_role`（对应 `SysUserRole` 实体、`/api/sys_user_role` 接口）维护，实现"给某用户增删角色"；**角色 ↔ 资源**的授权通过 `sys_role_res`（对应 `SysRoleRes` 实体、`/api/sys_role_res` 接口）维护，为角色分配可访问的菜单资源。二者共同落地 RBAC 的核心授权关系。

### 3.5 功能五：菜单管理

菜单管理控制器映射 `/api/sys_menu`，实体 `SysMenu` 通过 `parent_id` 自关联构成**树形菜单**，`type` 字段区分菜单（0）/ 页面（1）/ 资源（2）三级粒度，`icon`、`sort`、`component` 分别控制图标、排序与前端组件路径。菜单数据既是后台可维护的配置，也是前端导航渲染与 `sys_role_res` 授权的基础：角色被授予哪些菜单 id，登录后就能看到并进入对应功能。

> **小结**：用户管理、角色管理、菜单管理三者均以"通用 CRUD 基类 + JPA 实体映射"的方式实现，代码简洁、结构统一；再通过两张关联表把"用户—角色—菜单资源"串成完整的 RBAC 授权链路。

---

## 四、课程设计结果及分析测试

### 4.1 运行环境

- JDK 17、Maven 3.8+、MySQL 8.0+
- 启动后访问 `http://localhost:8080`，演示账号：`admin / admin123`（超级管理员）、`test / 123456`（测试用户）

### 4.2 功能测试

对本人负责的模块设计并执行如下测试用例：

| 编号 | 测试项 | 输入 / 操作 | 预期结果 | 实测 |
| --- | --- | --- | --- | --- |
| T1 | 正常登录 | admin / admin123 | 返回 200 + Token，跳转主控台 | ✅ 通过 |
| T2 | 密码错误 | admin / 错误口令 | 返回"密码错误"，停留登录页并提示 | ✅ 通过 |
| T3 | 账号不存在 | 不存在的账号 | 返回"账号不存在" | ✅ 通过 |
| T4 | 未登录访问 | 不带 Token 请求 `/api/sys_user` | 返回 401，前端跳回登录页 | ✅ 通过 |
| T5 | 携带令牌访问 | 带有效 Token 请求业务接口 | 正常放行返回数据 | ✅ 通过 |
| T6 | 退出登录 | 调用 logout 后再用旧 Token | 令牌失效，返回 401 | ✅ 通过 |
| T7 | 修改密码 | 原密码正确 + 新密码≥6 位 | 修改成功，需重新登录 | ✅ 通过 |
| T8 | 修改密码-弱口令 | 新密码 < 6 位 | 返回"新密码至少 6 位" | ✅ 通过 |
| T9 | 用户 CRUD | 新增 / 修改 / 删除用户 | 数据库对应记录随之变更 | ✅ 通过 |
| T10 | 角色 / 菜单 CRUD | 增删改角色、菜单 | 列表与数据库一致 | ✅ 通过 |
| T11 | 敏感字段过滤 | 登录 / 查询当前用户 | 返回体不含 password、salt | ✅ 通过 |
| T12 | 响应式样式 | 浏览器缩放至窄屏 | 侧边栏收起、登录页配图隐藏、布局不错乱 | ✅ 通过 |

### 4.3 结果分析

- **安全性达标**：密码全程加盐散列、绝不明文；接口统一鉴权，未登录一律拦截；响应剔除敏感字段——认证与授权链路完整闭环。
- **功能完整**：登录 / 退出 / 改密、用户 / 角色 / 菜单 CRUD、用户-角色 / 角色-资源分配均可正常工作，与数据库数据实时一致。
- **样式统一美观**：全站基于设计变量，深色侧栏 + 浅色工作区风格协调，登录页专业、响应式适配良好。
- **代码质量**：系统管理类接口通过泛型基类复用，冗余度低、可维护性高。

---

## 五、课程设计总结

本次课程设计中，本人负责**前端样式设计**与后端**登录认证、用户管理、角色管理、菜单管理**四个核心模块，完整实现了系统的"视觉皮肤"与"权限地基"。

**主要成果：**

1. 建立了一套基于 CSS 设计变量的统一视觉规范，落地了登录页与后台主控台两类界面，兼顾美观、一致与响应式。
2. 实现了"加盐散列 + 内存令牌 + 拦截器统一鉴权"的完整认证授权机制，做到密码不落明文、接口全站受控。
3. 基于 RBAC 模型，用 `sys_user / sys_role / sys_menu` 三张主表加 `sys_user_role / sys_role_res` 两张关联表，搭起"用户—角色—资源"三层授权体系，并通过泛型 CRUD 基类高效实现了三者的后台管理。

**收获与体会：**

- 深入理解了 **RBAC 权限模型**的设计思想——用"角色"解耦用户与权限，使授权更灵活可控；
- 掌握了 **Spring Boot 分层开发**与 **Spring Data JPA** 实体映射、拦截器、统一返回体等工程实践；
- 体会到**加盐散列**相较明文 / 单纯 MD5 的安全价值，以及"服务端剔除敏感字段"的防泄漏意识；
- 通过 **CSS 变量 + Grid/Flex 布局**，认识到设计规范化对多人协作与后期维护的重要意义。

**不足与改进方向：**

- 当前令牌保存在内存中，服务重启后失效、也不便多实例部署，后续可升级为 **JWT** 或引入 **Redis** 集中管理；
- 业务逻辑目前集中在 Controller，可进一步下沉到独立 **Service 层**，职责更清晰；
- 前端授权目前较为基础，可结合 `sys_role_res` 做到**菜单按角色动态渲染**与更细粒度的按钮级权限控制。

总体而言，本次课程设计让我完整走通了"前端样式—后端认证—权限模型—数据库设计"的全链路，既锻炼了工程实现能力，也加深了对 Web 系统安全与权限设计的理解，达到了课程设计的预期目标。
