# 仓库管理系统 —— 6 人开发分工（简版）

> 6 个人全部是**开发**，每人**全栈**负责自己的模块：Vue 页面 + 后端接口（Controller/Service/Mapper）+ 对应数据库表。
> 技术栈统一：Java 17 + Spring Boot + MySQL 8 + Vue + Element UI。
> 全部 18 张表按模块分给 6 人，互不重叠。

---

## 分工总表

| 开发 | 负责模块（具体功能） | 负责数据库表 |
| --- | --- | --- |
| **开发1<br>（权限）** | **登录认证**：账号+密码（salt 加密）登录校验、退出、登录态维护、初次登录强制改密<br>**用户管理**：用户增删改查、启用/锁定、重置密码、给用户分配角色<br>**角色管理**：角色增删改查、给角色分配菜单/权限<br>**菜单管理**：菜单树增删改查、菜单类型（菜单/页面/资源）维护、排序 | `sys_user`、`sys_role`、`sys_menu`、`sys_user_role`、`sys_role_res` |
| **开发2<br>（出入库）** | **出入库管理**：入库登记（type=0，库存+）、出库登记（type=1，库存−）、选择货物/数量/操作人/备注，实时同步 `goods.count`<br>**库存记录**：出入库流水查询、按货物/时间/出入库类型筛选、查看每笔明细 | `record`（读写 `goods.count`） |
| **开发3<br>（基础资料）** | **仓库管理**：仓库增删改查（仓库名、备注）<br>**商品分类**：分类增删改查（分类名、备注）<br>**商品档案**：商品增删改查（名称、所属仓库、分类、数量、备注）、按仓库/分类筛选查询 | `storage`、`goodstype`、`goods` |
| **开发4<br>（供应链）** | **供应商管理**：供应商增删改查（名称、联系人、电话、邮箱、地址）<br>**客户管理**：客户增删改查（收货联系人、地址等）<br>**批次/保质期**：批次登记（批次号、生产日期、到期日、关联供应商/货物）、临期货物提醒查询 | `supplier`、`customer`、`goods_batch` |
| **开发5<br>（库存运营）** | **库存预警**：设置每种货物的最低/最高库存阈值、启用/关闭预警、缺货与积压清单展示<br>**库存盘点**：新建盘点单（选仓库/盘点人）、录入实盘数量、自动算差异、完成盘点<br>**货位管理**：库位增删改查（所属仓库、库位编码如 A-01-01、名称） | `stock_alert`、`stock_check`、`stock_check_item`、`location` |
| **开发6<br>（统计与平台）** | **图表统计**：库存分布、出入库趋势等图表（汇总各业务表数据可视化）<br>**操作日志**：日志查询（操作内容、IP、调用方法、耗时、结果）<br>**系统公告**：公告发布/编辑/查看（草稿、发布状态）<br>**个人中心**：查看/修改个人信息、修改密码 | `sys_log`、`sys_notice` |

---

## 协作约定（简）

1. **框架先搭好**：由开发1 牵头搭 Spring Boot + Vue 基础框架（登录、路由、菜单加载），其余人基于统一框架开发各自模块。
2. **分支开发**：每人一个 Git 分支，模块完成后合并到主分支。
3. **表已就绪**：所有 18 张表见 `ahut_base_clean.sql` + `ahut_base_extend.sql`，各自只改自己负责的表。
4. **接口风格统一**：统一返回格式、统一权限校验（走开发1 的权限模块）。

---

## 分工是否均衡

| 开发 | 模块数 | 表数 | 备注 |
| --- | --- | --- | --- |
| 开发1 | 4 | 5 | 权限逻辑多 |
| 开发2 | 2 | 1 | 表少但核心逻辑重 |
| 开发3 | 3 | 3 | 基础 CRUD |
| 开发4 | 3 | 3 | 基础 CRUD |
| 开发5 | 3 | 4 | 盘点逻辑略多 |
| 开发6 | 4 | 2 | 前端图表偏多 |

整体按"模块内聚 + 工作量均衡"划分，人人有前端也有后端。
