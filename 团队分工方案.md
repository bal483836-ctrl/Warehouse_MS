# 仓库管理系统 —— 6 人开发分工（简版）

> 6 个人全部是**开发**，每人**全栈**负责自己的模块：Vue 页面 + 后端接口（Controller/Service/Mapper）+ 对应数据库表。
> 技术栈统一：Java 17 + Spring Boot + MySQL 8 + Vue + Element UI。
> 全部 18 张表按模块分给 6 人，互不重叠。

---

## 分工总表

| 开发（负责模块） | 负责数据库表 |
| --- | --- |
| **开发1**：登录认证、用户管理、角色管理、菜单管理 | `sys_user`、`sys_role`、`sys_menu`、`sys_user_role`、`sys_role_res` |
| **开发2**：出入库管理、库存记录 | `record`（+改 `goods.count`） |
| **开发3**：仓库管理、商品分类、商品档案 | `storage`、`goodstype`、`goods` |
| **开发4**：供应商管理、客户管理、批次/保质期 | `supplier`、`customer`、`goods_batch` |
| **开发5**：库存预警、库存盘点、货位管理 | `stock_alert`、`stock_check`、`stock_check_item`、`location` |
| **开发6**：图表统计、操作日志、系统公告、个人中心 | `sys_log`、`sys_notice` |

---

## 协作约定（简）

1. **框架先搭好**：由开发1 牵头搭 Spring Boot + Vue 基础框架（登录、路由、菜单加载），其余人基于统一框架开发各自模块。
2. **分支开发**：每人一个 Git 分支，模块完成后合并到主分支。
3. **表已就绪**：所有 18 张表见 `ahut_base_clean.sql` + `ahut_base_extend.sql`，各自只改自己负责的表。
4. **接口风格统一**：统一返回格式、统一权限校验（走开发1 的权限模块）。

---

## 分工是否均衡

| 开发 | 模块数 | 表数 | 备注 |
| --- | --- | --- | --- |
| 开发1 | 4 | 5 | 权限逻辑多 |
| 开发2 | 2 | 1 | 表少但核心逻辑重 |
| 开发3 | 3 | 3 | 基础 CRUD |
| 开发4 | 3 | 3 | 基础 CRUD |
| 开发5 | 3 | 4 | 盘点逻辑略多 |
| 开发6 | 4 | 2 | 前端图表偏多 |

整体按"模块内聚 + 工作量均衡"划分，人人有前端也有后端。
