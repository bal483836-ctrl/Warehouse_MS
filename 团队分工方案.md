# 仓库管理系统 —— 6 人开发分工（简版）

> 6 个人全部是**开发**，每人**全栈**负责自己的模块：Vue 页面 + 后端接口（Controller/Service/Mapper）+ 对应数据库表。
> 技术栈统一：Java 17 + Spring Boot + MySQL 8 + Vue + Element UI。
> 全部 18 张表按模块分给 6 人，互不重叠。

---

## 分工总表

| 开发 | 负责模块 | 负责数据库表 | 前端页面 |
| --- | --- | --- | --- |
| **开发1（权限）** | 登录认证、用户管理、角色管理、菜单管理 | `sys_user`、`sys_role`、`sys_menu`、`sys_user_role`、`sys_role_res` | Login、UserManage、role/Index、menu/Index |
| **开发2（出入库）** | 出入库管理、库存记录 | `record`（读写 `goods.count`） | GoodsManage、RecordManage |
| **开发3（基础资料）** | 仓库管理、商品分类、商品档案 | `storage`、`goodstype`、`goods` | StorageManage、GoodstypeManage |
| **开发4（供应链）** | 供应商管理、客户管理、批次/保质期 | `supplier`、`customer`、`goods_batch` | SupplierManage、CustomerManage、BatchManage |
| **开发5（库存运营）** | 库存预警、库存盘点、货位管理 | `stock_alert`、`stock_check`、`stock_check_item`、`location` | StockAlert、StockCheck、LocationManage |
| **开发6（统计与平台）** | 图表统计、操作日志、系统公告、个人中心 | `sys_log`、`sys_notice` | StatisticsChart、logs/Index、NoticeManage、UserInformation |

---

## 每个人具体做什么

### 开发1 · 权限模块（登录 + 用户/角色/菜单）
- 登录认证：账号密码 + salt 校验、生成登录态。
- 用户增删改查、给用户分配角色。
- 角色增删改查、给角色分配菜单权限。
- 菜单树的增删改查。
- 表：`sys_user`、`sys_role`、`sys_menu`、`sys_user_role`、`sys_role_res`。

### 开发2 · 出入库核心
- 入库（type=0）/ 出库（type=1）：写一条 `record`，同时增减 `goods.count`。
- 库存流水查询（按货物/时间/类型筛选）。
- 表：`record`（协同 `goods.count`）。
- 说明：这是系统最核心的业务逻辑，表少但逻辑重（注意库存数量同步）。

### 开发3 · 基础资料
- 仓库的增删改查。
- 商品分类的增删改查。
- 商品档案的增删改查（名称、所属仓库、分类、数量、备注）。
- 表：`storage`、`goodstype`、`goods`。

### 开发4 · 供应链
- 供应商管理（来源单位）。
- 客户管理（出库去向）。
- 批次/保质期管理（生产日期、到期日、临期提醒）。
- 表：`supplier`、`customer`、`goods_batch`。

### 开发5 · 库存运营
- 库存预警：设置每种货物的最低/最高库存，缺货或积压时提示。
- 库存盘点：新建盘点单、录入实盘数量、算差异。
- 货位管理：仓库内货架/库位编码维护。
- 表：`stock_alert`、`stock_check`、`stock_check_item`、`location`。

### 开发6 · 统计与平台
- 图表统计：库存 / 出入库数据可视化（读各业务表汇总）。
- 操作日志：接口调用日志查询。
- 系统公告：发布/查看内部公告。
- 个人中心：查看和修改个人信息、改密码。
- 表：`sys_log`、`sys_notice`。

---

## 协作约定（简）

1. **框架先搭好**：由开发1 牵头搭 Spring Boot + Vue 基础框架（登录、路由、菜单加载），其余人基于统一框架开发各自模块。
2. **分支开发**：每人一个 Git 分支，模块完成后合并到主分支。
3. **表已就绪**：所有 18 张表见 `ahut_base_clean.sql` + `ahut_base_extend.sql`，各自只改自己负责的表。
4. **接口风格统一**：统一返回格式、统一权限校验（走开发1 的权限模块）。

---

## 分工是否均衡

| 开发 | 模块数 | 表数 | 备注 |
| --- | --- | --- | --- |
| 开发1 | 4 | 5 | 权限逻辑多 |
| 开发2 | 2 | 1 | 表少但核心逻辑重 |
| 开发3 | 3 | 3 | 基础 CRUD |
| 开发4 | 3 | 3 | 基础 CRUD |
| 开发5 | 3 | 4 | 盘点逻辑略多 |
| 开发6 | 4 | 2 | 前端图表偏多 |

整体按"模块内聚 + 工作量均衡"划分，人人有前端也有后端。
