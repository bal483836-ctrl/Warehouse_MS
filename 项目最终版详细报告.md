# 小明智库 · 超市仓储进销存管理系统 —— 最终版详细报告

> 版本：最终重构版（Batch 1 ~ Batch 4 全部落地）
> 技术栈：Spring Boot 3.3.5 + Spring Data JPA + MySQL 8 + 原生 HTML/CSS/JS 单页应用
> 代码分支：`claude/warehouse-batch-2-orders-igk9ll`
> 登录演示账号：`admin / admin123`、`test / 123456`

---

## 目录

1. [项目概述](#一项目概述)
2. [总体架构与技术选型](#二总体架构与技术选型)
3. [功能设计详解（逐模块）](#三功能设计详解逐模块)
4. [数据库设计（逐表逐列）](#四数据库设计逐表逐列)
5. [后端代码解读（逐类）](#五后端代码解读逐类)
6. [前端代码解读（SPA 架构）](#六前端代码解读spa-架构)
7. [核心业务数据流（互联互通）](#七核心业务数据流互联互通)
8. [安全设计与操作日志](#八安全设计与操作日志)
9. [部署与运行](#九部署与运行)
10. [版本演进史（Batch 1 ~ 4）](#十版本演进史batch-14)
11. [测试验证情况与已知边界](#十一测试验证情况与已知边界)
12. [附录：项目目录结构](#十二附录项目目录结构)

---

## 一、项目概述

### 1.1 项目定位

**小明智库**是一套面向**超市 / 日用品仓库**的进销存（仓储）管理系统。目标用户是超市运营者、外部超市仓库的仓管人员。系统的核心价值主张：

> 让使用者清楚地知道**每个阶段、每个商品**的「库存多少、卖出多少、买入多少」，
> 从而**优化仓储、精准进货、科学选品**。

### 1.2 解决的实际问题

| 传统痛点 | 系统对策 |
|---|---|
| 库存数字靠人记，不准确 | 库存只随「入库/出库」操作变动，商品编辑界面**不允许直接改库存** |
| 不知道货放在哪 | 入库时**强制选择落位库位**（货架），且库位分区（冷冻/冰鲜/普通）必须与商品分区匹配 |
| 临期商品发现晚、损耗大 | 商品档案维护**保质期天数 + 清理预警提前天数**，入库自动算到期日，系统**自动生成清理提示** |
| 缺货发现晚、补货不及时 | 每个商品可设**安全下限**，低于即触发**补货提示**（右上角铃铛 + 预警看板） |
| 进货/卖货没有单据链路 | **采购订单/销售订单**双单据体系，出入库可关联订单，形成完整链路 |
| 操作无痕、出错无法追溯 | 所有写操作（新增/修改/删除/出入库/取消订单等）**自动落库操作日志** |

### 1.3 系统边界（有意为之的取舍）

- 面向**单店/单仓小团队**使用：不做复杂的多角色权限树（原菜单管理、角色资源、用户角色等模块已裁撤），保留登录鉴权 + 个人资料 + 修改密码。
- 库存盘点、批次管理作为**独立模块被裁撤**，保质期能力**并入商品档案与入库流程**（更贴近超市实际操作习惯）。
- 客户管理模块裁撤：销售订单直接记录**买家**（toC 门店散客场景），不再强制先建客户档案。

---

## 二、总体架构与技术选型

### 2.1 架构总览

```
┌────────────────────────────────────────────────────────┐
│  浏览器（单页应用 SPA）                                  │
│  index.html + app.js(763行) + app.css(286行)            │
│  hash 路由 · 就地视图+面包屑 · SVG 手绘图表 · 铃铛提示    │
└───────────────▲────────────────────────────────────────┘
                │ fetch JSON（请求头携带 token）
┌───────────────┴────────────────────────────────────────┐
│  Spring Boot 3.3.5（内嵌 Tomcat，端口 8080）             │
│  ├─ security：AuthInterceptor(鉴权) + AccessLog(日志)    │
│  ├─ controller：25 个控制器（REST /api/**）              │
│  ├─ repo：21 个 Spring Data JPA 仓库接口                 │
│  └─ entity：21 个 JPA 实体                               │
└───────────────▲────────────────────────────────────────┘
                │ JPA / JdbcTemplate
┌───────────────┴────────────────────────────────────────┐
│  MySQL 8（库名 ahut_base，utf8mb3/utf8mb4）              │
│  基础 18 表 + orders/order_item + 各批次增量列            │
└────────────────────────────────────────────────────────┘
```

### 2.2 技术选型与理由

| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | Spring Boot 3.3.5（Java 17） | 约定优于配置，内嵌 Tomcat 一键运行；生态成熟，答辩演示零外部依赖 |
| ORM | Spring Data JPA（Hibernate） | 实体↔表自动映射，`BaseController<T>` 一套泛型代码即可给全部表提供 CRUD |
| 复杂查询 | JdbcTemplate 原生 SQL | 统计聚合（趋势、KPI、货位看板、提示生成）用 SQL 表达最直接、性能可控 |
| 数据库 | MySQL 8 | 课程/生产两相宜；脚本兼容 MariaDB 10 |
| 前端 | 原生 HTML/CSS/JS（零框架、零构建） | 打开即用，无 node 构建链；763 行 JS 实现完整 SPA，便于讲解每一行代码 |
| 鉴权 | 自研 Token（内存会话）+ MD5 加盐 | 演示系统规模合适；结构上可平滑替换为 JWT/Redis |
| 图表 | 手绘 SVG（无 ECharts） | 折线/环形图共 3 个函数 ≈120 行，零依赖、可讲清原理 |

### 2.3 工程结构（Maven 单模块）

```
backend/
├─ pom.xml                     # spring-boot-starter-web / data-jpa / validation / mysql / lombok
└─ src/main/
   ├─ java/com/wms/
   │  ├─ WmsApplication.java   # 启动类
   │  ├─ common/               # Result 统一响应 / BaseController 泛型CRUD / Md5Util
   │  ├─ config/               # WebConfig(拦截器+CORS) / JacksonConfig(日期格式)
   │  ├─ security/             # TokenStore / AuthInterceptor / AccessLogInterceptor
   │  ├─ entity/  (21 个)      # JPA 实体
   │  ├─ repo/    (21 个)      # JpaRepository 接口
   │  └─ controller/ (25 个)   # REST 控制器
   └─ resources/
      ├─ application.yml       # 数据源 / JPA(ddl-auto:none) / 上传体积上限
      └─ static/               # 前端：index.html / login.html / app.js / app.css / img/**
```

---

## 三、功能设计详解（逐模块）

前端导航共 6 组 11 个入口，全部页面采用「**就地视图 + 面包屑返回**」交互（详情、表单不再弹窗，直接替换主内容区），仅保留两个轻量对话框：删除确认、订单取消原因。

### 3.1 登录页（login.html）

- **宽版两栏布局**：左侧登录表单（品牌 Logo、标题「欢迎登录 · 小明智库」、账号密码选项卡、演示账号提示），右侧仓库实景大图 + 渐变遮罩 + 宣传语。
- **密码显隐**：密码框尾部小眼睛图标，点击在 `password/text` 间切换，激活时变主题蓝。
- 登录成功：`POST /api/auth/login` 返回 `token + user`，存入 `localStorage`，跳转 `index.html`；失败时按钮下方红字提示。
- 已登录访问 login.html 自动跳首页；任何接口返回 401 时前端自动清空本地并回到登录页。

### 3.2 运营总览（#/dashboard）

- **5 个 KPI 卡**：总库存量、今日入库、今日出库、库存预警数、仓位使用率——全部由 `GET /api/stats/kpi` 用 SQL 实时聚合（不是写死的数字）。
- **出入库趋势**：按月聚合 `record` 表，最多取最近 12 个有数据的月份；面积+双折线（入库蓝/出库绿），悬停有数值气泡。
- **库存分类占比**：环形图 + 图例（分类名/数量/百分比），来自 `goodstype LEFT JOIN goods` 聚合。
- **近期出入库记录**：最新 8 条流水，货品名、分区徽章、入/出类型、±数量、操作人、时间。
- 顶栏问候按时段变化（凌晨好/上午好/中午好/下午好/晚上好）。
- ⚠ 按需求 **已移除** 旧版「库位热力图」板块（非真实必要信息）。

### 3.3 预警看板（#/alerts）

数据源与铃铛一致（`GET /api/notices`），分两栏：

- **补货预警**：`库存 < 安全下限` 的商品，文案形如「牙膏」库存 90，低于安全下限 120，需及时进货。
- **清理预警**：`到期日 ≤ 今天 + 商品清理提前天数` 的货位批次，文案形如：货位 鲜-5-01 的「葡萄」(500) 将于 2 天后到期，需清理；已过期的行标红（crit 级）。
- 每行点击直接跳到对应**商品详情页**。

### 3.4 出入库操作（#/inout）

入库表单字段：货物、数量、**落位库位**、关联订单（可选）、**收货凭证图片**（可选）、备注。

- **落位库位联动**：选择货物后，库位下拉自动过滤为「该商品所属仓库 + 与商品分区一致」的库位；若该分区没有库位，给出明确提示引导去货位管理添加。
- **保质期自动计算**：若商品档案设置了保质期天数，入库时自动按 `入库时间 + 保质期` 写入该批次的到期日（存进 `location_stock.expiry_date`）。
- **收货凭证**：本地图片选择后浏览器端压缩（最长边 900px、JPEG 85%）为 dataURL 随流水入库。
- **出库**：校验库存充足后按「**先到期先出**」原则跨批次扣减（到期早的批次先扣，无到期日的最后扣）。
- 右侧「当前库存」表：货物/分区/仓库/保质期/库存；下方「最新流水」表含落位库位与关联订单号。
- 每次操作成功后：刷新库存、流水、铃铛提示（保证预警实时）。

### 3.5 采购订单（#/orders/0）

- 表格式列表：订单号（可点进详情）、供应商、商品项数、总量、金额、状态（待处理/已完成/已取消）、下单时间。
- 新建/编辑为**就地表单**：选择供应商、状态、备注 + **多行商品明细**（商品/数量/单价，可增删行）；金额由后端按明细自动合计。
- 订单号自动生成：`PO + yyyyMMddHHmmss`。
- 采购订单允许删除（连带删除明细）。

### 3.6 销售订单（#/orders/1）★ 重点重构

- **外卖订单式卡片流**（非表格）：每张卡片显示——
  - 头部：订单号 + 状态徽章 + 下单时间；
  - 信息行：**买家**（toC 散客/店名文本）、**经手账号**（自动记录当前登录用户）、金额；
  - 明细区（虚线分隔）：每行商品名 ×数量 单价；
  - 操作区：编辑 /「**取消订单**」。
- **不可删除**：前端不提供删除按钮，后端 `DELETE` 对 type=1 直接拒绝（双保险）。
- **取消流程**：点「取消订单」弹出确认框，**必须填写取消原因**才能提交（`POST /api/orders/{id}/cancel`），成功后卡片置灰显示「已取消：原因 · 时间」，且不可再编辑。
- 支持按订单号 / 买家搜索。

### 3.7 商品档案（#/t/goods）

- **图片卡片网格**：商品图 + 名称 + 分区徽章 + 库存徽章（**低于安全下限时红底白字**）。
- 表单字段：名称、分类、仓库、存储分区（冷冻/冰鲜/普通）、**保质期天数**（留空=不易过期）、**到期前预警清理天数**、商品图片（本地上传自动压缩）、备注。
- **编辑不允许修改库存**：表单中没有库存字段，并有提示「库存由出入库操作管理」；新增商品库存从 0 开始。
- **商品详情页（#/goods/{id}）**：
  - 左图右信息：分类/分区/仓库/当前库存（低则红+⚠）/安全下限/保质期/清理预警/供应商（从采购订单+批次记录自动推导）；
  - **出入库/库存历史折线图**：三条线（入库蓝、出库橙、库存绿），支持 **周/月/季/年** 四种粒度切换；
  - 近期出入库记录表。

### 3.8 货位管理（#/t/location）★ 重点重构

- 按**分区分组的看板**（冷冻→冰鲜→普通排序），每个库位一张卡片：
  - 库位编码 + 所属仓库；
  - **使用率进度条**（绿色渐变，≥95% 变橙红）；
  - 「已用 x/容量」与「**剩余容量**」；
  - **已存商品清单**：每行商品名 ×数量（空库位显示"空置"）。
- 新增/编辑：编码、名称、所属仓库、分区、排/列、容量。
- 数据接口 `GET /api/location/board` 一次算好 used/remain/usage/items。

### 3.9 供应商（#/t/supplier）

- 图片卡片：供应商图/Logo + 名称 + 联系人电话。
- 详情页（#/supplier/{id}）：联系信息 + **所供商品卡片**（由「该供应商的采购订单明细 ∪ 批次记录」推导），点商品卡直接进入商品详情（与商品档案同一逻辑复用）。

### 3.10 预警阈值（#/t/stock_alert）

- 表格管理每个商品的：安全下限、库存上限、**状态（启用/停用，中文徽章）**、备注。
- 该表直接驱动：铃铛补货提示、预警看板、商品卡片红色徽章、KPI 预警数。

### 3.11 操作日志（#/t/sys_log）★ 真实落库

- 列：操作内容（如「入库」「新增商品档案」「取消订单」）、操作账号、IP、接口（`POST /api/inout/in`）、结果（HTTP 状态）、耗时(ms)、时间。
- 由 `AccessLogInterceptor` 对**所有写请求**自动记录，无需业务代码配合；列表默认显示最近 200 条，支持搜索。

### 3.12 个人资料（#/profile）

- 左卡：头像（姓名首字）、姓名、**ID**、登录账号、电话。
- 右卡：**修改密码**——原密码 + 新密码（≥6 位）+ 确认；后端校验原密码（MD5 加盐比对）后按原盐重新散列保存；成功后强制重新登录。

### 3.13 系统提示铃铛（顶栏）★ 系统公告的新形态

- 顶栏铃铛图标 + 红色数字徽标（提示条数）。
- 下拉面板实时列出两类提示（与预警看板同源）：
  - 🟠 **补货提示**：「洁面乳」库存 82，低于安全下限…（我编的示例文案格式如上节）
  - 🔴 **清理提示**：货位 鲜-5-01 的「葡萄」(500) 将于 2 天后到期，需清理。
- 点击条目跳转对应商品详情；出入库等操作后自动刷新。

### 3.14 全局交互规范

- **少弹窗**：详情/新增/编辑全部就地渲染，顶部**面包屑**（如 `商品档案 / 编辑`）可点击返回；
- 路由切换有 loading 动画；Esc 关闭对话框；
- 移动端（≤1100px）侧边栏变**抽屉**（汉堡按钮 + 遮罩）；≤560px 栅格降为单列；
- 文案全中文（状态、按钮、空态、错误提示）。

---

## 四、数据库设计（逐表逐列）

库名 `ahut_base`。一键脚本 `db/ahut_base_full.sql`（建库 + 建账号 `wms/wms123456` + 全部表结构与演示数据 + Batch2/3/4 增量），模块化脚本按批次拆分（`ahut_base_clean.sql` 基础 → `ahut_base_extend.sql` 扩展 → `heatmap_seed / record_seed` → `batch2 → batch3 → batch3_images → batch4`），`db/init_db.sh` 按序执行。

### 4.1 核心业务表（9 张）

#### ① goods —— 商品档案（系统中心表）

| 列 | 类型 | 说明 |
|---|---|---|
| id | int PK AI | 主键 |
| name | varchar(100) | 商品名 |
| storage | int | 所属仓库 → storage.id（逻辑外键） |
| goodsType | int | 分类 → goodstype.id |
| count | int | **当前库存**（只由出入库接口变更） |
| zone | varchar(10) 默认'普通' | **存储分区：冷冻/冰鲜/普通**，决定可落位的库位 |
| image | mediumtext | 商品图片（dataURL 或 `/img/goods/{id}.jpg` 路径） |
| shelf_life_days | int NULL | **保质期天数**；NULL=不易过期（如数码产品） |
| cleanup_warn_days | int 默认7 | **到期前多少天触发清理预警** |
| remark | varchar(1000) | 备注 |

演示数据 9 种商品：iPhone14、iPad Air5（无保质期）、洁面乳(730天)、牙膏(1095天)、AD钙(270天)、旺仔牛奶(180天)、葡萄(10天/提前3天)、西红柿(12天/提前3天)、皮皮虾(30天/提前5天，冷冻)。

#### ② goodstype —— 商品分类
`id / name / remark`。演示：日用品、数码产品、食品、冷冻品、水果、蔬菜等。

#### ③ storage —— 仓库
`id / name / remark`。演示：数码仓库(2)、食品仓库(3)、**生鲜仓库(5，三分区演示仓)**、日用品仓库(10)。

#### ④ location —— 货位（货架）

| 列 | 类型 | 说明 |
|---|---|---|
| id | int PK AI | 主键 |
| storage_id | int | 所属仓库 |
| code | varchar(50) | 库位编码，如 `鲜-5-01`、`冻-5-02`、`普-10-03` |
| name | varchar(100) | 名称，如「冰鲜1排」 |
| zone | varchar(10) | **分区：冷冻/冰鲜/普通** |
| row_no / col_no | int | 排 / 列 |
| capacity | int 默认500 | 库位容量（件） |
| remark | varchar(1000) | 备注 |

演示 30 个库位：数码仓普通×8、食品仓普通×6、**生鲜仓 冷冻×3+冰鲜×4+普通×3**、日用品仓普通×6。

#### ⑤ location_stock —— 库位存货（批次粒度）★

| 列 | 类型 | 说明 |
|---|---|---|
| id | int PK AI | 主键 |
| location_id | int | 库位 → location.id |
| goods_id | int | 商品 → goods.id |
| count | int | 该批次在该库位的数量 |
| inbound_date | datetime | **该批次入库日期** |
| expiry_date | datetime NULL | **该批次到期日期**（入库时=入库时间+商品保质期） |

> 设计要点：一次入库=一条批次记录（同商品在同库位可有多条不同到期日的批次），这是「先到期先出」和「清理预警」的数据基础。出库扣减到 0 的批次记录会被删除。

#### ⑥ record —— 出入库流水

| 列 | 类型 | 说明 |
|---|---|---|
| id | int PK AI | 主键 |
| goods | int | 商品 → goods.id |
| user_id | int | 操作人 → sys_user.id |
| count | int | 数量（正数） |
| type | int | **0=入库，1=出库** |
| createtime | datetime | 操作时间 |
| order_id | int NULL | **关联订单** → orders.id |
| location_id | int NULL | **入库落位库位** → location.id |
| image | mediumtext | **入库收货凭证图片** |
| remark | varchar(1000) | 备注 |

#### ⑦ orders —— 订单主表（采购+销售统一）★

| 列 | 类型 | 说明 |
|---|---|---|
| id | int PK AI | 主键 |
| order_no | varchar(50) | 单号：采购 `PO+时间戳`、销售 `SO+时间戳` |
| type | int | **0=采购订单，1=销售订单** |
| supplier_id | int NULL | 供应商（采购单）→ supplier.id |
| customer_id | int NULL | 历史兼容保留（客户模块已裁撤） |
| buyer | varchar(100) | **买家**（销售单 toC：散客·王先生 / 店名） |
| operator_id | int | **经手账号** → sys_user.id（创建时自动记录） |
| status | int | **0=待处理，1=已完成，2=已取消** |
| total_amount | decimal(12,2) | 金额（按明细自动合计） |
| order_time | datetime | 下单时间 |
| cancel_reason | varchar(500) | **取消原因**（取消时必填） |
| cancel_time | datetime | 取消时间 |
| remark | varchar(1000) | 备注 |
| 索引 | | idx_type / idx_supplier / idx_customer |

#### ⑧ order_item —— 订单明细

`id / order_id(idx) / goods_id / count / price decimal(10,2) / remark`。一张订单多行商品。

#### ⑨ stock_alert —— 库存预警阈值

`id / goods_id / min_count(安全下限) / max_count(上限) / enabled(1启用0停用) / remark`。
演示：牙膏 90<120（轻度缺口）、AD钙 400<800（中度缺口）——保证开箱即有红色预警可看。

### 4.2 支撑表

| 表 | 用途 | 现状 |
|---|---|---|
| supplier | 供应商（含 **image** mediumtext 图片列、联系人/电话/邮箱/地址、del_flag 软删、审计列） | 使用中 |
| sys_user | 用户（number 账号、password+salt 加盐散列、locked、del_flag、审计列） | 登录/个人资料使用 |
| sys_log | 操作日志（content 操作内容、ip_addr、user_id 存**账号字符串**、methods 接口、result 状态码、duration 耗时ms、create_time） | **由拦截器真实写入** |
| customer / goods_batch / stock_check(+item) / sys_role / sys_menu / sys_role_res / sys_user_role / sys_notice | 早期批次遗留的兼容保留表 | 前端已裁撤入口，表保留不影响运行 |

### 4.3 表关系（逻辑外键，无硬约束——沿用原库风格）

```
goodstype 1─N goods N─1 storage 1─N location
                │                    │
                │ 1─N location_stock N─1   （批次：goods×location×到期日）
                │ 1─N record N─1 sys_user
                │         └─N─1 location（入库落位）
                │         └─N─1 orders 1─N order_item N─1 goods
                │                  └─N─1 supplier（采购单）
                └─1─1 stock_alert
```

### 4.4 增量脚本演进

| 脚本 | 内容 |
|---|---|
| batch2.sql | goods+zone/image；record+order_id/image；建 **orders/order_item**；库位按三分区重建+同分区放置存货；预警阈值造出深浅红 |
| batch3.sql | supplier+image；补日化供应商采购单（丰富"所供商品"推导） |
| batch3_images.sql | 商品/供应商 image 指向内置图片路径（真实照片放入 `/img/**` 即自动替换，缺图回退矢量图） |
| batch4.sql | goods+shelf_life_days/cleanup_warn_days；location_stock+inbound_date/expiry_date；record+location_id；orders+buyer/operator_id/cancel_reason/cancel_time；生鲜批次补临期数据（葡萄 2 天、西红柿 3 天到期→开箱即有清理预警）；补 2 张 toC 散客销售单 |

---

## 五、后端代码解读（逐类）

### 5.1 公共基座（common/）

**`Result<T>`** —— 统一响应结构 `{code, msg, data}`；`code=200` 成功；静态工厂 `ok()/fail()`。前端据此统一处理。

**`BaseController<T>`** —— 泛型 CRUD 基类，5 个端点（GET 列表 / GET{id} / POST / PUT{id} / DELETE{id}）。子类只需两行：注入对应 `Repo` 并实现 `repo()`。系统里 goods、goodstype、storage、location、supplier、stock_alert、record、sys_log 等十余个控制器都由它派生——**这是 25 个控制器却只有约 2200 行 Java 的关键**。

**`Md5Util`** —— 口令散列：`md5(md5(password) + salt)`，与原库存量口令兼容。

### 5.2 安全层（security/）

**`TokenStore`** —— 内存会话表 `ConcurrentHashMap<token, Principal(userId,name,number)>`；`issue` 发 UUID token、`resolve` 解析、`revoke` 注销。

**`AuthInterceptor`** —— 拦截 `/api/**`（放行 login），请求头 `token` 解析失败即返回 401 JSON；成功则把 `Principal` 挂到 request 供业务取用（如出入库记录操作人、订单记录经手人）。

**`AccessLogInterceptor`** ★ —— 操作日志真实落库：
- `preHandle` 记录开始时间；`afterCompletion` 里只处理 **POST/PUT/DELETE**（排除登录）；
- 把 URI 翻译成中文动作：`/inout/in→入库`、`/inout/out→出库`、`/{id}/cancel→取消订单`、`changePassword→修改密码`、其余按 `新增/修改/删除 + 资源中文名`（内置 13 个资源名映射）；
- 写入 sys_log：动作、账号（从 Principal 取 number）、IP（优先 X-Forwarded-For）、`方法+URI`、HTTP 状态、耗时 ms；
- 全程 try-catch 吞异常——**日志失败绝不影响业务主流程**。

### 5.3 认证（AuthController）

- `login`：查账号→校验 del_flag/locked→加盐散列比对→发 token，返回脱敏用户（不含 password/salt）。
- `me`：按 token 返回当前用户。
- `changePassword` ★：校验原密码→新密码≥6 位→按**原盐**重新散列保存。
- `logout`：吊销 token。

### 5.4 出入库核心（InOutController）★ 全系统最关键的业务类

```java
public record MoveReq(Integer goodsId, Integer count, String remark,
                      Integer orderId, Integer locationId, String image) {}
```

**入库 `POST /api/inout/in`（@Transactional）**：
1. 参数校验（正数数量、商品存在）；
2. `pickLocation`：确定落位库位——用户指定的库位必须**属于商品所属仓库且分区一致**，否则拒绝；未指定时自动挑该仓库同分区第一个库位；找不到则返回中文引导（"该分区暂无可用库位，请先在货位管理中添加"）；
3. `goods.count += n` 保存；
4. `allocate`：**新建一条批次记录** `location_stock(location, goods, n, 入库时间, 到期日)`，其中 `到期日 = 商品有保质期 ? now + shelf_life_days : NULL`；
5. `writeRecord`：写流水（操作人来自 token Principal、订单关联、落位库位、凭证图片）。

**出库 `POST /api/inout/out`（@Transactional）**：
1. 库存充足性校验（"库存不足，当前库存 x"）；
2. `goods.count -= n`；
3. `deallocate` ——**先到期先出（FEFO）**：把该商品所有批次按 `expiry_date ASC（NULL 视为最大）, id ASC` 排序，逐批扣减；批次扣到 0 即删除该批次行；
4. 写流水（出库不带落位与图片）。

> 一致性保障：两个接口都在**单个事务**内同时更新 `goods.count`、`location_stock` 批次、`record` 流水，三处数据永远同步。

### 5.5 订单（OrderController）

- `list(type?)`：按类型过滤倒序；每单附 items、itemCount、totalQty、operatorName（联表 sys_user 取经手人姓名）。
- `create`：单号自动 `PO/SO+yyyyMMddHHmmss`；`operator_id` 自动=当前登录人；`saveItems` 重算 `total_amount = Σ(price×count)`。
- `update`：**已取消订单禁止修改**；明细全量替换（先删后插）再重算金额。
- `cancel` ★：`POST /{id}/cancel`，**原因必填**（空串拒绝）；置 status=2、写 cancel_reason/cancel_time。
- `delete` ★：**type=1（销售订单）直接拒绝**——"销售订单不可删除，请使用「取消」"；采购单连带删除明细。

### 5.6 系统提示（NoticeController）★ 铃铛/预警看板数据源

`GET /api/notices` 返回两类提示（纯 SQL 实时生成，无需定时任务）：

```sql
-- 补货：库存低于安全下限
SELECT ... FROM stock_alert sa JOIN goods g ON g.id=sa.goods_id
WHERE sa.enabled=1 AND g.count < sa.min_count
ORDER BY (sa.min_count - g.count) DESC;         -- 缺口越大越靠前

-- 清理：批次到期日 ≤ 今天 + 商品的清理提前天数
SELECT ..., DATEDIFF(ls.expiry_date, NOW()) days
FROM location_stock ls JOIN goods g ... JOIN location l ...
WHERE ls.expiry_date IS NOT NULL AND ls.count>0
  AND ls.expiry_date <= NOW() + INTERVAL COALESCE(g.cleanup_warn_days,7) DAY
ORDER BY ls.expiry_date ASC;
```

文案在 Java 侧拼装：过期 `days<0` 为 crit 级（"已过期 n 天"），未到期为 warn 级（"将于 n 天后到期 / 今日到期"）；每条带 goodsId 供前端跳详情。

### 5.7 货位看板（LocationController.board）

`GET /api/location/board`：库位联仓库名 + 每库位聚合 `location_stock`（商品名/数量/最早到期日）→ 计算 `used / remain=max(0,capacity-used) / usage%`，一次返回前端直接渲染。

### 5.8 统计与档案（StatsController / ProfileController）

- `stats/kpi`：总库存、今日入/出（`DATE(createtime)=CURDATE()`）、预警数、仓位使用率（Σ存货/Σ容量）。
- `stats/trend`：按月聚合流水（入/出两序列，最多 12 期）。
- `stats/category`：分类库存占比。
- `profile/goods/{id}`：商品详情 + 安全下限 + **供应商推导**（该商品出现过的采购单供应商 ∪ 批次表供应商，UNION 去重）。
- `profile/goods/{id}/history?period=week|month|quarter|year` ★：
  - 期键分别用 `YEARWEEK(,3) / DATE_FORMAT('%Y%m') / CONCAT(YEAR,QUARTER) / YEAR` 聚合，取最近 8/12/8/5 期；
  - **库存回推算法**：只存了流水没存每期库存快照，于是从**当前库存**出发倒着推——`stock[i-1] = stock[i] - (入[i] - 出[i])`，得到每期期末库存序列。三条序列（入/出/存）一次返回。
- `profile/supplier/{id}/goods`：该供应商所供商品（采购明细 ∪ 批次，含库存与安全下限，供卡片标红）。

### 5.9 配置（config/）

- `WebConfig`：注册两个拦截器（鉴权→日志）+ CORS 全放行（便于前后端分离调试）。
- `JacksonConfig`：LocalDateTime/LocalDate 统一 `yyyy-MM-dd HH:mm:ss` / `yyyy-MM-dd`，关闭时间戳序列化。
- `application.yml`：`ddl-auto: none`（**表结构以 SQL 脚本为准**，杜绝 JPA 偷偷改表）；Tomcat/multipart 上限 20MB/12MB（图片上传兜底）。

---

## 六、前端代码解读（SPA 架构）

`app.js` 763 行，一个 IIFE 闭包完成整个应用。无框架、无构建。

### 6.1 启动流程

```
init() → buildNav()（渲染侧边栏+顶栏+铃铛+抽屉事件）
       → loadRefs()（并行拉 storage/goodstype/goods/supplier 四类字典）
       → 监听 hashchange / Esc / resize → route()
```

### 6.2 hash 路由表

| 路由 | 视图 |
|---|---|
| `#/dashboard` | 运营总览 |
| `#/alerts` | 预警看板 |
| `#/inout` | 出入库操作 |
| `#/orders/0` / `#/orders/1` | 采购订单表格 / 销售订单卡片流 |
| `#/order/{id}` | 订单详情（面包屑返回） |
| `#/t/goods` → `#/goods/{id}` | 商品卡片网格 → 商品详情+历史折线 |
| `#/t/supplier` → `#/supplier/{id}` | 供应商卡片 → 详情+所供商品 |
| `#/t/location` | 货位看板 |
| `#/t/record` `#/t/goodstype` `#/t/stock_alert` `#/t/sys_log` | 通用表格引擎 |
| `#/profile` | 个人资料+改密码 |

`route()` 先渲染 loading 转圈，再按前缀分发；未匹配显示"页面不存在"；异常统一显示"加载失败：msg"（401 除外——直接回登录页）。

### 6.3 通用「就地表单」引擎 renderForm(view, opts)

一个函数支撑全站所有新增/编辑（商品/供应商/库位/分类/预警阈值/订单外）：

- 字段描述式声明：`{k, label, type: text|number|select|ref|textarea|image, options|list, full}`；
- `ref` 类型渲染字典下拉（如仓库/分类/商品）；`image` 类型渲染 隐藏input + 文件选择 + 预览，选择后走 `readImageScaled` 压缩；
- 顶部面包屑 + 可选提示条（如「库存由出入库操作管理，此处不可修改」）；
- 保存：收集 `[data-f]` 值 → `opts.onSubmit(obj)` → 成功后刷新字典并 hash 跳回列表。

### 6.4 图片处理

- `readImageScaled(file, cb)`：FileReader→Image→canvas 等比缩放（最长边 900px）→ `toDataURL('image/jpeg', .85)`；非图片文件 toast 拒绝；异常兜底用原图。
- `imgTag(src)`：`/img/**.jpg` 加载失败时 `onerror` 自动回退同名 `.svg` 矢量占位图——**放真实照片即显示照片，不放也绝不裂图**。

### 6.5 手绘 SVG 图表（零依赖）

- `smooth(pts)`：Catmull-Rom → 贝塞尔平滑路径（单点/空数据有兜底，修过稀疏数据 bug）；
- `drawTrend`：总览双序列面积折线；`drawLineChart`：详情页**三序列**折线（入/出/存），周月季年切换只换数据重画；`drawDonut`：环形占比。
- 三者共享网格/坐标轴/悬浮气泡（`showTip` 定位在鼠标旁，防溢出）。

### 6.6 对话框与提示

- `confirmDialog(msg)` / `promptDialog(title,label,ph)`：Promise 化的轻量对话框，全站仅用于**删除确认**与**取消订单填原因**；Esc/点遮罩关闭。
- `showToast`：顶部居中 2.2s 自动消失，错误红底。
- 铃铛：`refreshBell()` 拉 `/api/notices` 更新徽标与面板；出入库成功后主动刷新。

### 6.7 样式体系（app.css 286 行）

CSS 变量主题（品牌深蓝 `#0a0e17~#2a78d6`、语义色 good/warn/crit 及 wash 淡底）；组件类：`.kpi .card .pill .pcard(商品卡) .oc(销售订单卡) .loc-card(货位卡) .bell-* .crumbs .seg(周月季年切换) .modal-bg`；响应式两档断点（1100px 抽屉导航、560px 单列）。

---

## 七、核心业务数据流（互联互通）

**链路 A · 进货**：
供应商 →（新建**采购订单** PO，多行明细，金额自动合计）→ 出入库**入库**（选该商品分区匹配的**落位库位**、可关联 PO、可传**凭证照片**）→ 同一事务内：`goods.count+`、新增**批次**（含到期日）、写**流水**（操作人/落位/订单）→ 货位看板即刻可见「该库位存了什么、剩余多少」。

**链路 B · 销售**：
新建**销售订单** SO（买家文本 + 经手账号自动记录）→ 出库（可关联 SO）→ **先到期先出**跨批次扣减 → 流水/历史折线/KPI 同步更新；订单可**取消**（必填原因，留痕）。

**链路 C · 补货预警闭环**：
`stock_alert` 设安全下限 → 出库使 `goods.count` 跌破下限 → 铃铛/预警看板/商品卡红徽章/KPI **同时**出现 → 按提示进货入库 → 库存回升，提示自动消失。

**链路 D · 清理预警闭环**：
商品档案设保质期与提前天数 → 入库批次自动算到期日 → 到期临近：铃铛+预警看板出现"货位 X 的「商品」(n) 将于 n 天后到期，需清理" → 出库/清理该批次 → 提示消失。

四条链路没有任何 mock 数据：页面上每个数字都可追溯到 `goods / location_stock / record / orders / stock_alert` 五张表的真实行。

---

## 八、安全设计与操作日志

| 项 | 方案 |
|---|---|
| 口令存储 | `md5(md5(pwd)+salt)`，每用户独立盐；改密码沿用原盐重散列 |
| 会话 | 服务端内存 token（UUID），请求头 `token` 携带；注销即吊销 |
| 接口保护 | `/api/**` 全拦截（登录除外）；401 统一 JSON；前端收到 401 自动清会话回登录页 |
| 敏感字段 | 登录/me 返回的用户对象剔除 password/salt |
| XSS | 前端所有动态文本经 `esc()` HTML 转义后再插入 |
| 操作留痕 | 全部写操作自动入 sys_log（动作中文名/账号/IP/接口/状态/耗时/时间） |
| 上传防护 | 前端仅接受 image/* 且压缩；服务端请求体上限兜底 |

---

## 九、部署与运行

### 9.1 IDEA 源码运行（推荐答辩演示）

1. `mysql -uroot -p < db/ahut_base_full.sql`（一次导入：建库/建账号/全部表+演示数据）；
2. IDEA 打开 `backend/`（识别 pom.xml，等依赖下载）；
3. 运行 `WmsApplication.main()`；
4. 浏览器 `http://localhost:8080`，`admin/admin123` 登录。

### 9.2 免构建运行（dist/）

`warehouse-ms.jar`（内嵌前端）+ `ahut_base_full.sql` + `application.yml`（外置可改库账号端口）+ `start.sh / 启动.bat` + 启动说明。

### 9.3 一键脚本

`run.sh`（Maven 打包+启动）、`db/init_db.sh`（按批次顺序重建数据库）。

---

## 十、版本演进史（Batch 1~4）

| 批次 | 主题 | 关键交付 |
|---|---|---|
| Batch 1 | 品牌与骨架 | 更名、登录两栏+密码显隐、导航精简、预警看板雏形、种子精简、趋势图稀疏数据修复 |
| Batch 2 | 订单链路+分区 | orders/order_item 双表、出入库关联订单+凭证、库位三分区+商品分区约束、低库存"越少越红" |
| Batch 3 | 档案与分析 | 商品/供应商图片卡片与详情页、周月季年三线历史折线（库存回推算法）、图片上传（压缩）、内置配图+回退 |
| Batch 4（最终） | 真实业务重构 | 销售订单外卖卡片+取消必填原因+禁删、保质期/清理预警体系、入库落位+FEFO 出库、货位看板（存了什么/剩多少）、操作日志落库、个人资料+改密码、铃铛提示、全站就地视图+面包屑、裁撤非核心模块 |

---

## 十一、测试验证情况与已知边界

**已验证**（云端开发环境无 MySQL，验证到构建层）：
- 后端 `mvn package` 全量编译通过；关键类（Order/Notice/Location/InOut/AccessLog）均产出 class 并打入 jar；
- 前端 `node --check` 语法通过；字段名与后端 JSON（camelCase）逐一核对；
- SQL 脚本与实体列名逐一对账（含 `ONLY_FULL_GROUP_BY`、mediumtext 容量、索引）；
- dist jar 解包抽查：新前端与新控制器已在包内。

**待真机点验清单**（导库后重点走一遍）：
1. 入库选落位 → 货位看板出现对应批次；
2. 生鲜出库 → 批次按到期日先扣；
3. 销售订单取消 → 原因/时间留痕、卡片置灰；
4. 铃铛：初始即应有「牙膏/AD钙补货」+「葡萄/西红柿清理」四条演示提示；
5. 改密码后旧密码失效；
6. 操作日志出现上述全部动作。

**已知边界**：
- Token 存内存：重启服务需重新登录（可平滑升级 JWT/Redis）；
- 无分页：演示数据量级（各表 ≤ 数十行）下全量加载，日志侧已截最近 200 条；
- 逻辑外键无级联：删除商品不会自动清理其流水/批次（沿用原库风格，答辩可作为"改进方向"陈述）。

---

## 十二、附录：项目目录结构

```
Warehouse_MS/
├─ backend/                    # Spring Boot 工程（源码运行入口）
│  └─ src/main/java/com/wms/   # 25 controller / 21 entity / 21 repo / common / config / security
│  └─ src/main/resources/static# 前端 SPA + img/goods|suppliers 配图
├─ db/                         # ahut_base_full.sql 一键脚本 + batch2/3/4 增量 + init_db.sh
├─ dist/                       # 免构建运行包（jar + SQL + 配置 + 启动脚本）
├─ 画图/                       # E-R 图、功能图、流程图（历史设计图）
├─ 截图/                       # 旧版系统截图（演进对照）
├─ README-IDEA.md              # IDEA 导入运行指南
├─ 项目最终版详细报告.md        # 本报告
├─ 小明智库-答辩汇报.pptx       # 答辩 PPT（配套生成）
└─ ppt.pptx / 仓库管理系统.docx # 旧版汇报材料（参考）
```

---

*报告完。配套答辩 PPT 见仓库根目录 `小明智库-答辩汇报.pptx`。*
